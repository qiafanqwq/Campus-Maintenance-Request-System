<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.cmrs.mapper.UserMapper">

    <select id="adminPageQuery" resultType="UserVO">
        select * from user2 where authority_id in (1,3)
        <if test="nickname != null and nickname != ''">
            and nickname like concat('%',#{nickname},'%')
        </if>
        order by id asc
    </select>

    <select id="repairmanPageQuery" resultType="UserVO">
        select * from user2 where authority_id = 2
        <if test="nickname != null and nickname != ''">
            and nickname like concat('%',#{nickname},'%')
        </if>
        order by id asc
    </select>

    <select id="userPageQuery" resultType="UserVO">
        select * from user1
        <where>
            <if test="nickname != null and nickname != ''">
                nickname like concat('%',#{nickname},'%')
            </if>
        </where>
        order by id asc
    </select>


    <update id="updateNormalUser" parameterType="User">
        update user1
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="password != null">password = #{password},</if>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        where id = #{id}
    </update>

    <update id="updateAdminOrRepairman" parameterType="User">
        update user2
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="password != null">password = #{password},</if>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="status != null">status = #{status},</if>
            <if test="AuthorityId != null">authority_id = #{AuthorityId},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        where id = #{id}
    </update>


    <select id="getNormalUserById2" resultType="UserVO">
        select * from user1 where id = #{id}
    </select>

    <select id="getAdminOrRepairmanById2" resultType="UserVO">
        select * from user2 where id = #{id}
    </select>

    <select id="listRepairman" resultType="UserVO">
        select * from user2 where status = 1
    </select>
</mapper>